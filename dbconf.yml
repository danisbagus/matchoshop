development:
    driver: postgres
    dsn: postgres://postgres:mypass@localhost:8010/matchoshop?sslmode=disable
    migrationsDir: app/migrations

production:
    driver: postgres
    dsn: ${DATABASE_URL}?sslmode=${DB_SSL_MODE}
    migrationsDir: app/migrations