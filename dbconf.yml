development:
    driver: postgres
    dsn: ${DATABASE_URL}?sslmode=${DB_SSL_MODE}
    migrationsDir: db/migrations

production:
    driver: postgres
    dsn: ${DATABASE_URL}?sslmode=${DB_SSL_MODE}
    migrationsDir: db/migrations